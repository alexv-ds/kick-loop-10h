# kick-loop-10h

1) git clone https://github.com/alexv-ds/kick-loop-10h.git
2) pip install -r requirements.txt
3) change `duration_sec` in `main.py` if needed. By default it's set to 10 hours, which uses about 13 GB of RAM. I couldn't be bothered to optimize it.
4) pyton3 main.py
5) ...
6) profit
